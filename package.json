{
  "name": "ai-video-ad-maker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "remotion",
    "shared"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "lint": "echo \"Add linting per package\"",
    "setup": "echo \"Install workspace dependencies\""
  },
  "dependencies": {
    "pip": "^0.0.1"
  }
}
