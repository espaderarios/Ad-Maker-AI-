services:
  server:
    build:
      context: ./server
    env_file:
      - ./server/.env
    ports:
      - "4000:4000"

  client:
    build:
      context: ./client
    env_file:
      - ./client/.env
    ports:
      - "5173:5173"
